Retrieval-augmented generation combines a language model with an external knowledge store. When a user asks a question, the system retrieves the most relevant documents from a vector index and supplies them to the language model as grounded context. This reduces hallucination, makes knowledge easy to update without retraining, and gives the model access to information outside its training cutoff. Popular frameworks include LangChain and LlamaIndex.
